// #include <libc.h>
// #include "cub.h"

// int main()
// {
//     int i = -1;

//     char *r = "   EA       ";
//     char *str = ft_strtrim(r, " ");
//     printf("-%s-", str);
// }

#include <stdio.h>
#include <math.h>

int main() {

    int a = (int)4294967294;
    printf("%d\n", a);
    // float angle = M_PI / 2; // 90 درجة، يعني اتجاه للأعلى في الرياضيات
    // float speed = 10;

    // // حركة المحور y في الرياضيات (y يزيد للأعلى)
    // float dy_math = sin(angle) * speed;
    // printf("في الرياضيات (y للأعلى): dy = %.2f\n", dy_math);

    // // حركة المحور y في الشاشة (y يزيد للأسفل)، نحاكيه بعكس الإشارة
    // float dy_screen = -sin(angle) * speed;
    // printf("في الشاشة (y للأسفل): dy = %.2f\n", dy_screen);

    return 0;
}